syntax = "proto3";

package comm;

message node {
    string name = 1;
    string ip = 2;

    enum state {
        UP = 0;
        DOWN = 1;
        UNKNOWN = 2;
        }

    state status  = 3;
  }